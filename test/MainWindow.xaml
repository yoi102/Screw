<Window x:Class="test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:test"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Grid.Column="0" Text="{Binding Com_Para1.Name}"/>
        <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding Com_Para1.Height}"/>

        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Com_Para2.Name}"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Com_Para2.Height}"/>

        <Button Grid.Row="2" Grid.Column="2" Command="{Binding SaveComand}"/>

    </Grid>
</Window>
